# Python 3.11+ recommended.
# Python 3.13 is compatible with most packages below; see TensorFlow note at the bottom.

numpy>=1.26
pandas>=2.1
scipy>=1.11
scikit-learn>=1.3
pyfixest>=0.18         # Paper 0005 estimator: feols() with CRV1 clustered SEs
statsmodels>=0.14      # Paper 0017 estimator: OLS with explicit design matrix
lightgbm>=4.1          # MILGBM imputation method (Method 7)
openpyxl>=3.1          # Excel output (pd.ExcelWriter engine="openpyxl")
pyreadstat>=1.2        # Reading .dta source data (Paper 0005 only)
tensorflow>=2.13       # Required for Method 6 (Deep Learning imputation via MLP)

# If tensorflow is unavailable at import time, the DL method falls back to mean imputation.
# Installation may fail on Windows due to the Long Path limitation —
# enable it via registry (HKLM\SYSTEM\CurrentControlSet\Control\FileSystem\LongPathsEnabled=1)
# before installing.
